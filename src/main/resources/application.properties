# Database Configuration
spring.profiles.active=dev

# Database Configuration (Tự động chuyển đổi: Dùng Cloud nếu có biến môi trường, ngược lại dùng Local)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/quan_ly_phong_tro?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/

# Logging
logging.level.com.example.quanliPT=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${MAIL_USERNAME:dattruongquoc78@gmail.com}
spring.mail.password=${MAIL_PASSWORD:lzeoouqpbewooiot}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
# Telegram Configuration
app.telegram.bot-token=${TELEGRAM_BOT_TOKEN:}

# Frontend Redirect Configuration (Cho thanh toán PayOS / Stripe)
app.frontend.url=${FRONTEND_URL:http://localhost:3000}

# PayOS Configuration
payos.clientId=${PAYOS_CLIENT_ID:07a3480e-65f6-481b-93c7-90bb3e01b28e}
payos.apiKey=${PAYOS_API_KEY:e31e1fcc-ba45-4460-bc0a-542a467706bb}
payos.checksumKey=${PAYOS_CHECKSUM_KEY:61f5c3b171e082eaa7cf4ecce1fb4d8f7a185e6b68cfd94260ddfb7d23ff6231}

# Stripe Configuration
stripe.secret.key=${STRIPE_SECRET_KEY:}
stripe.publishable.key=${STRIPE_PUBLISHABLE_KEY:}

# Gemini AI Configuration
gemini.api.key=${GEMINI_API_KEY:}
gemini.model=${GEMINI_MODEL:gemini-3.6-flash}

# Upload folder. On deployment, point APP_UPLOAD_DIR to a persistent disk/folder.
app.upload-dir=${APP_UPLOAD_DIR:uploads}

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}
